# Unlock YouTube Comments website

Before publishing:
1. Replace `YOUR-EMAIL@example.com` in `privacy.html` with your support email.
2. Confirm the Chrome Web Store URL in `index.html`.
3. Upload all files and the `images` folder to the repository root.
